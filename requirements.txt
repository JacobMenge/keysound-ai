# Direkte Abhaengigkeiten. Getestet mit Python 3.12 unter Windows 11.
# Installation:  pip install -r requirements.txt
#
# torch: Unter Windows und macOS kommt hier die CPU-Version. Unter Linux holt
# pip sonst das mehrere GB grosse CUDA-Paket - dort vorher die CPU-Version
# installieren (Befehl im README). Das Netz hat rund 24 000 Parameter und
# trainiert auf einem normalen Rechner in unter zwei Minuten; eine GPU bringt
# bei dieser Groesse nichts.

numpy>=1.26
scipy>=1.11
matplotlib>=3.8
soundfile>=0.12
sounddevice>=0.4.6
torch>=2.2
pillow>=10.0
